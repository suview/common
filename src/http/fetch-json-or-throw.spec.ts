import fetchJsonOrThrow from './fetch-json-or-throw';

describe('http/fetchJsonOrThrow', () => {
    it('does something', () => {
        expect(fetchJsonOrThrow()).toEqual('fetch-json-or-throw');
    });
});
