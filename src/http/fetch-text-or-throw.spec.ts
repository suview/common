import fetchTextOrThrow from './fetch-text-or-throw';

describe('http/fetchTextOrThrow', () => {
    it('does something', () => {
        expect(fetchTextOrThrow()).toEqual('fetch-text-or-throw');
    });
});
