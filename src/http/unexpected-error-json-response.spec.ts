import unexpectedErrorJsonResponse from './unexpected-error-json-response';

describe('http/unexpectedErrorJsonResponse', () => {
    it('does something', () => {
        expect(unexpectedErrorJsonResponse()).toEqual('unexpected-error-json-response');
    });
});
