import fromSentenceCase from './from-sentence-case';

describe('string/fromSentenceCase', () => {
    it('does something', () => {
        expect(fromSentenceCase()).toEqual('from-sentence-case');
    });
});